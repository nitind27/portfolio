import SupportPageClient from '@/components/marketing/SupportPageClient';

export const metadata = {
  title: 'Support & Feedback — site99',
  description: 'Raise a complaint, share feedback, or contact site99 support.',
};

export default function SupportPage() {
  return <SupportPageClient />;
}
